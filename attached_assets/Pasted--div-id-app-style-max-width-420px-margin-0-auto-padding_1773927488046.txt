
<div id="app" style="max-width: 420px; margin: 0 auto; padding: 1rem 0;">
  <div id="phone-frame" style="border: 0.5px solid var(--color-border-secondary); border-radius: var(--border-radius-xl); overflow: hidden; background: var(--color-background-primary);">
    
    <div style="background: #FF7900; padding: 12px 16px; display: flex; align-items: center; gap: 10px;">
      <div style="font-size: 15px; font-weight: 500; color: #fff; flex: 1;">orange.fr</div>
      <div id="step-indicator" style="font-size: 12px; color: rgba(255,255,255,0.8);"></div>
    </div>

    <div id="content" style="padding: 20px 16px; min-height: 420px;"></div>
  </div>
  <p style="text-align: center; font-size: 12px; color: var(--color-text-tertiary); margin-top: 12px;">Prototype interactif — projet design thinking IMT BS</p>
</div>

<script>
const state = { step: 0, foyer: null, usages: [], operateur: null, budget: null };

const usageOptions = [
  { id: 'streaming', label: 'Streaming vidéo', desc: 'Netflix, Disney+, YouTube...' },
  { id: 'gaming', label: 'Jeux en ligne', desc: 'Fortnite, LoL, Steam...' },
  { id: 'teletravail', label: 'Télétravail / visio', desc: 'Teams, Zoom, VPN...' },
  { id: 'basique', label: 'Navigation basique', desc: 'Mails, réseaux sociaux...' }
];

const operateurs = ['SFR', 'Free', 'Bouygues', 'Sosh', 'Autre'];

function render() {
  const c = document.getElementById('content');
  const si = document.getElementById('step-indicator');
  
  if (state.step === 0) {
    si.textContent = '';
    c.innerHTML = `
      <div style="text-align: center; padding: 30px 0 20px;">
        <div style="width: 56px; height: 56px; border-radius: 50%; background: #FF7900; margin: 0 auto 16px; display: flex; align-items: center; justify-content: center;">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
        </div>
        <h2 style="font-size: 20px; font-weight: 500; margin: 0 0 8px; color: var(--color-text-primary);">Mon conseiller Orange</h2>
        <p style="font-size: 14px; color: var(--color-text-secondary); margin: 0 0 24px; line-height: 1.5;">Trouvez l'offre idéale pour votre foyer en 4 questions.<br>Recommandation personnalisée, prix transparent.</p>
        <button onclick="state.step=1;render()" style="background: #FF7900; color: #fff; border: none; padding: 12px 32px; border-radius: 24px; font-size: 15px; font-weight: 500; cursor: pointer;">C'est parti</button>
        <p style="font-size: 12px; color: var(--color-text-tertiary); margin-top: 16px;">Moins de 2 minutes, sans engagement</p>
      </div>`;
    return;
  }

  if (state.step === 1) {
    si.textContent = '1/4';
    c.innerHTML = `
      <p style="font-size: 13px; color: #FF7900; font-weight: 500; margin: 0 0 4px;">Question 1/4</p>
      <h3 style="font-size: 18px; font-weight: 500; margin: 0 0 6px; color: var(--color-text-primary);">Combien de personnes utilisent internet chez vous ?</h3>
      <p style="font-size: 13px; color: var(--color-text-secondary); margin: 0 0 20px;">Mobiles + box comprises</p>
      <div id="foyer-opts" style="display: flex; flex-direction: column; gap: 10px;"></div>`;
    const opts = document.getElementById('foyer-opts');
    [{v:1,l:'Moi seul(e)'},{v:2,l:'2 personnes'},{v:3,l:'3-4 personnes'},{v:5,l:'5 personnes ou +'}].forEach(o => {
      const btn = document.createElement('button');
      btn.textContent = o.l;
      btn.style.cssText = 'background: var(--color-background-secondary); border: 0.5px solid var(--color-border-tertiary); border-radius: var(--border-radius-md); padding: 14px 16px; font-size: 14px; text-align: left; cursor: pointer; color: var(--color-text-primary);';
      btn.onmouseover = () => btn.style.borderColor = '#FF7900';
      btn.onmouseout = () => btn.style.borderColor = 'var(--color-border-tertiary)';
      btn.onclick = () => { state.foyer = o.v; state.step = 2; render(); };
      opts.appendChild(btn);
    });
    return;
  }

  if (state.step === 2) {
    si.textContent = '2/4';
    c.innerHTML = `
      <p style="font-size: 13px; color: #FF7900; font-weight: 500; margin: 0 0 4px;">Question 2/4</p>
      <h3 style="font-size: 18px; font-weight: 500; margin: 0 0 6px; color: var(--color-text-primary);">Quels sont vos usages principaux ?</h3>
      <p style="font-size: 13px; color: var(--color-text-secondary); margin: 0 0 20px;">Sélectionnez tout ce qui s'applique</p>
      <div id="usage-opts" style="display: flex; flex-direction: column; gap: 10px;"></div>
      <button id="usage-next" onclick="if(state.usages.length){state.step=3;render();}" style="background: #FF7900; color: #fff; border: none; padding: 12px 0; border-radius: 24px; font-size: 14px; font-weight: 500; cursor: pointer; width: 100%; margin-top: 20px; opacity: 0.4;">Continuer</button>`;
    const opts = document.getElementById('usage-opts');
    state.usages = [];
    usageOptions.forEach(u => {
      const btn = document.createElement('button');
      btn.innerHTML = `<span style="font-size: 14px; font-weight: 500; color: var(--color-text-primary);">${u.label}</span><br><span style="font-size: 12px; color: var(--color-text-secondary);">${u.desc}</span>`;
      btn.style.cssText = 'background: var(--color-background-secondary); border: 0.5px solid var(--color-border-tertiary); border-radius: var(--border-radius-md); padding: 12px 16px; text-align: left; cursor: pointer;';
      let selected = false;
      btn.onclick = () => {
        selected = !selected;
        if (selected) { state.usages.push(u.id); btn.style.borderColor = '#FF7900'; btn.style.background = '#FFF5EC'; }
        else { state.usages = state.usages.filter(x=>x!==u.id); btn.style.borderColor = 'var(--color-border-tertiary)'; btn.style.background = 'var(--color-background-secondary)'; }
        document.getElementById('usage-next').style.opacity = state.usages.length ? '1' : '0.4';
      };
      opts.appendChild(btn);
    });
    return;
  }

  if (state.step === 3) {
    si.textContent = '3/4';
    c.innerHTML = `
      <p style="font-size: 13px; color: #FF7900; font-weight: 500; margin: 0 0 4px;">Question 3/4</p>
      <h3 style="font-size: 18px; font-weight: 500; margin: 0 0 6px; color: var(--color-text-primary);">Vous êtes actuellement chez quel opérateur ?</h3>
      <p style="font-size: 13px; color: var(--color-text-secondary); margin: 0 0 20px;">Pour votre box internet principale</p>
      <div id="op-opts" style="display: flex; flex-direction: column; gap: 10px;"></div>`;
    const opts = document.getElementById('op-opts');
    operateurs.forEach(op => {
      const btn = document.createElement('button');
      btn.textContent = op;
      btn.style.cssText = 'background: var(--color-background-secondary); border: 0.5px solid var(--color-border-tertiary); border-radius: var(--border-radius-md); padding: 14px 16px; font-size: 14px; text-align: left; cursor: pointer; color: var(--color-text-primary);';
      btn.onmouseover = () => btn.style.borderColor = '#FF7900';
      btn.onmouseout = () => btn.style.borderColor = 'var(--color-border-tertiary)';
      btn.onclick = () => { state.operateur = op; state.step = 4; render(); };
      opts.appendChild(btn);
    });
    return;
  }

  if (state.step === 4) {
    si.textContent = '4/4';
    c.innerHTML = `
      <p style="font-size: 13px; color: #FF7900; font-weight: 500; margin: 0 0 4px;">Question 4/4</p>
      <h3 style="font-size: 18px; font-weight: 500; margin: 0 0 6px; color: var(--color-text-primary);">Quel est votre budget mensuel idéal ?</h3>
      <p style="font-size: 13px; color: var(--color-text-secondary); margin: 0 0 20px;">Tout compris : box + mobiles</p>
      <div id="budget-opts" style="display: flex; flex-direction: column; gap: 10px;"></div>`;
    const opts = document.getElementById('budget-opts');
    [{v:'low',l:'Moins de 50€/mois'},{v:'mid',l:'50€ à 80€/mois'},{v:'high',l:'80€ à 120€/mois'},{v:'flex',l:'Pas de limite, je veux le meilleur'}].forEach(b => {
      const btn = document.createElement('button');
      btn.textContent = b.l;
      btn.style.cssText = 'background: var(--color-background-secondary); border: 0.5px solid var(--color-border-tertiary); border-radius: var(--border-radius-md); padding: 14px 16px; font-size: 14px; text-align: left; cursor: pointer; color: var(--color-text-primary);';
      btn.onmouseover = () => btn.style.borderColor = '#FF7900';
      btn.onmouseout = () => btn.style.borderColor = 'var(--color-border-tertiary)';
      btn.onclick = () => { state.budget = b.v; state.step = 5; render(); };
      opts.appendChild(btn);
    });
    return;
  }

  if (state.step === 5) {
    si.textContent = '';
    const isGamer = state.usages.includes('gaming');
    const isStreamer = state.usages.includes('streaming');
    const isTeletravail = state.usages.includes('teletravail');
    const bigFoyer = state.foyer >= 3;
    
    let offreName, offrePrice, offrePromoPrice, raisons = [], inclus = [];
    
    if ((isGamer || isTeletravail) && bigFoyer) {
      offreName = 'Livebox Max + Pack Open';
      offrePromoPrice = 54.99; offrePrice = 72.99;
      raisons = ['Débit symétrique 5 Gbit/s pour le gaming et la visio simultanés','Wi-Fi 6E avec répéteur inclus — couverture optimale pour un foyer de '+state.foyer+' pers.','Remise Open de -12€/mois par ligne mobile ajoutée'];
      inclus = ['Fibre 5 Gbit/s ↓ / 1 Gbit/s ↑','Répéteur Wi-Fi 6E inclus','Décodeur TV 4K (optionnel, retirable)','Appels illimités fixes + mobiles'];
    } else if (isGamer || isTeletravail) {
      offreName = 'Livebox Max';
      offrePromoPrice = 34.99; offrePrice = 54.99;
      raisons = ['Débit symétrique idéal pour le gaming / la visio','Latence optimisée par rapport à la Livebox standard','Meilleur rapport qualité/prix pour un usage intensif solo'];
      inclus = ['Fibre 5 Gbit/s ↓ / 1 Gbit/s ↑','Wi-Fi 6E intégré','Appels illimités fixes + mobiles'];
    } else if (bigFoyer) {
      offreName = 'Livebox Up + Pack Open';
      offrePromoPrice = 44.99; offrePrice = 62.99;
      raisons = ['Wi-Fi 6 couvre l\'ensemble du foyer de '+state.foyer+' pers.','Streaming fluide sur plusieurs écrans simultanés','Remise Open de -12€/mois par ligne mobile'];
      inclus = ['Fibre 2 Gbit/s partagé','Répéteur Wi-Fi inclus','160 chaînes TV','Appels illimités fixes + mobiles'];
    } else {
      offreName = 'Livebox';
      offrePromoPrice = 24.99; offrePrice = 42.99;
      raisons = ['Suffisant pour votre usage (navigation + streaming occasionnel)','Le meilleur prix pour une connexion fiable','Pas de surcoût pour des options que vous n\'utiliseriez pas'];
      inclus = ['Fibre 500 Mbit/s','Wi-Fi 5','Appels illimités vers les fixes','Décodeur TV en option (+5€)'];
    }
    
    const cout12 = Math.round(offrePromoPrice * 6 + offrePrice * 6);
    const coutMensuelMoyen = Math.round(cout12 / 12);
    
    c.innerHTML = `
      <div style="text-align: center; margin-bottom: 16px;">
        <div style="width: 40px; height: 40px; border-radius: 50%; background: #E8F5E9; margin: 0 auto 10px; display: flex; align-items: center; justify-content: center;">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#2E7D32" stroke-width="2.5" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        <p style="font-size: 13px; color: var(--color-text-secondary); margin: 0;">Notre recommandation pour vous</p>
      </div>

      <div style="background: var(--color-background-secondary); border: 2px solid #FF7900; border-radius: var(--border-radius-lg); padding: 16px; margin-bottom: 16px;">
        <div style="display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 12px;">
          <h3 style="font-size: 17px; font-weight: 500; margin: 0; color: var(--color-text-primary);">${offreName}</h3>
          <div style="text-align: right;">
            <span style="font-size: 22px; font-weight: 500; color: #FF7900;">${offrePromoPrice}€</span><span style="font-size: 12px; color: var(--color-text-secondary);">/mois</span>
            <br><span style="font-size: 11px; color: var(--color-text-tertiary);">puis ${offrePrice}€/mois</span>
          </div>
        </div>
        ${inclus.map(i => `<div style="font-size: 13px; color: var(--color-text-secondary); padding: 3px 0;">✓ ${i}</div>`).join('')}
      </div>

      <div style="background: #FFF8F0; border-radius: var(--border-radius-md); padding: 14px; margin-bottom: 16px;">
        <p style="font-size: 13px; font-weight: 500; color: #E65100; margin: 0 0 8px;">Pourquoi cette offre ?</p>
        ${raisons.map(r => `<p style="font-size: 13px; color: #BF360C; margin: 0; padding: 3px 0; line-height: 1.4;">→ ${r}</p>`).join('')}
      </div>

      <div style="background: var(--color-background-secondary); border-radius: var(--border-radius-md); padding: 14px; margin-bottom: 20px;">
        <p style="font-size: 13px; font-weight: 500; color: var(--color-text-primary); margin: 0 0 8px;">Coût réel sur 12 mois</p>
        <div style="display: flex; justify-content: space-between; font-size: 13px; color: var(--color-text-secondary); padding: 3px 0;">
          <span>6 premiers mois (promo)</span><span>${Math.round(offrePromoPrice * 6)}€</span>
        </div>
        <div style="display: flex; justify-content: space-between; font-size: 13px; color: var(--color-text-secondary); padding: 3px 0;">
          <span>6 mois suivants (plein tarif)</span><span>${Math.round(offrePrice * 6)}€</span>
        </div>
        <div style="display: flex; justify-content: space-between; font-size: 14px; font-weight: 500; color: var(--color-text-primary); padding: 8px 0 0; border-top: 0.5px solid var(--color-border-tertiary); margin-top: 6px;">
          <span>Coût mensuel moyen réel</span><span style="color: #FF7900;">${coutMensuelMoyen}€/mois</span>
        </div>
      </div>

      <button onclick="state.step=6;render()" style="width: 100%; background: #FF7900; color: #fff; border: none; padding: 13px; border-radius: 24px; font-size: 14px; font-weight: 500; cursor: pointer; margin-bottom: 10px;">Souscrire cette offre</button>
      <button onclick="state.step=0;render()" style="width: 100%; background: transparent; color: var(--color-text-secondary); border: 0.5px solid var(--color-border-tertiary); padding: 11px; border-radius: 24px; font-size: 13px; cursor: pointer;">Voir les autres offres / recommencer</button>
    `;
    return;
  }

  if (state.step === 6) {
    si.textContent = '';
    c.innerHTML = `
      <div style="text-align: center; padding: 40px 0;">
        <div style="width: 56px; height: 56px; border-radius: 50%; background: #E8F5E9; margin: 0 auto 16px; display: flex; align-items: center; justify-content: center;">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#2E7D32" stroke-width="2.5" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        <h3 style="font-size: 18px; font-weight: 500; margin: 0 0 8px; color: var(--color-text-primary);">Prototype terminé</h3>
        <p style="font-size: 14px; color: var(--color-text-secondary); line-height: 1.5; margin: 0 0 24px;">Dans la version réelle, le client serait redirigé vers le tunnel de souscription Orange pré-rempli avec l'offre recommandée.</p>
        <button onclick="state.step=0;state.foyer=null;state.usages=[];state.operateur=null;state.budget=null;render()" style="background: #FF7900; color: #fff; border: none; padding: 12px 32px; border-radius: 24px; font-size: 14px; font-weight: 500; cursor: pointer;">Recommencer la démo</button>
      </div>`;
  }
}
render();
</script>
